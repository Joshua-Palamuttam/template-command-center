# Personal configuration (contains names, orgs, channels, etc.)
config.yaml

# Context files are personal and ephemeral — don't commit
context/daily/*.md
context/weekly/*.md
context/goals/*.md
context/calendar/*.md
context/calibration.md

# Session scratch files
context/notes/scratch-*

# OS files
.DS_Store
Thumbs.db
